PORT=3001
HOST=127.0.0.1

JWT_SECRET="finish-work-early-local-jwt-secret-20260419"
JWT_EXPIRES_IN="7d"
ENABLE_DEMO_AUTH="true"
SEED_ADMIN_PASSWORD="Phase2Admin123!"

DATABASE_URL="postgresql://finishwork:finishwork_dev@127.0.0.1:5432/finish_work_early?schema=public"

# Local preview does not require SMTP. Codes will be logged to the backend console.
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
